resources:
  genie_spaces:
    jira_genie_space:
      title: ${var.genie_space_name}
      description: "Natural-language Q&A over governed Jira delivery metric views"
      warehouse_id: ${var.warehouse_id}
      file_path: ../../src/genie/jira_genie.geniespace.json
