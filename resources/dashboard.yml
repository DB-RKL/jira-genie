resources:
  dashboards:
    jira_analytics_dashboard:
      display_name: "Jira Analytics"
      file_path: ../dashboards/jira_analytics.lvdash.json
      warehouse_id: ${var.warehouse_id}
      parent_path: /Users/${var.owner_email}
