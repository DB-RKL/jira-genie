resources:
  schemas:
    bronze:
      catalog_name: ${var.catalog}
      name: ${var.bronze_schema}
      comment: "Raw landing from Lakeflow Connect Jira connector (28 source tables)"
    silver:
      catalog_name: ${var.catalog}
      name: ${var.silver_schema}
      comment: "Fivetran-parity Jira ERD (normalized, deduped, typed)"
    gold:
      catalog_name: ${var.catalog}
      name: ${var.gold_schema}
      comment: "Analytical marts: sprint velocity, cycle time, portfolio health, team productivity"
    metrics:
      catalog_name: ${var.catalog}
      name: ${var.metrics_schema}
      comment: "Business semantics: Unity Catalog metric views (governed dimensions + measures)"
